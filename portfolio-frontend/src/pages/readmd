<div className="min-h-screen bg-gray-100 p-8">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-3xl font-bold text-gray-900 mb-6">
          Welcome to Your Portfolio{user?.fullName?.firstName ? `, ${user.fullName.firstName}` : ''}!
        </h1>
        
        <div className="bg-white rounded-lg shadow-md p-6">
          <h2 className="text-xl font-semibold mb-4">Dashboard</h2>
          {user ? (
            <div className="space-y-4">
              <p><strong>Email:</strong> {user.email}</p>
              <p><strong>Name:</strong> {user.fullName?.firstName} {user.fullName?.lastName}</p>
              <p className="text-green-600">You are successfully logged in!</p>
            </div>
          ) : (
            <p className="text-red-500">No user data found. Please log in.</p>
          )}
        </div>

        <div className="mt-8 bg-white rounded-lg shadow-md p-6">
          <h2 className="text-xl font-semibold mb-4">Your Portfolio Sections</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div className="border rounded-lg p-4">
              <h3 className="font-semibold">Projects</h3>
              <p className="text-gray-600">Manage your projects</p>
            </div>
            <div className="border rounded-lg p-4">
              <h3 className="font-semibold">Experience</h3>
              <p className="text-gray-600">Add your work experience</p>
            </div>
            <div className="border rounded-lg p-4">
              <h3 className="font-semibold">Education</h3>
              <p className="text-gray-600">Manage your education</p>
            </div>
            <div className="border rounded-lg p-4">
              <h3 className="font-semibold">Contact</h3>
              <p className="text-gray-600">Update contact information</p>
            </div>
          </div>
        </div>
      </div>
    </div>